from django.apps import AppConfig

class EquipmentCheckerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'equipment_checker'
    verbose_name = 'Equipment Checker'
